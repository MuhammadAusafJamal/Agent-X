import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function KnowledgePage() {
  return (
    <PhasePlaceholder
      title="Knowledge"
      phase="Phase 5 — Agent"
      description="What the agent has learned about each application, with confidence, provenance, and a way to correct it."
    />
  );
}
