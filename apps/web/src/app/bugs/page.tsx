import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function BugsPage() {
  return (
    <PhasePlaceholder
      title="Bugs"
      phase="Phase 6 — Heal"
      description="Defects the diagnoser attributed to the application under test, with reproduction steps taken from the run that found them."
    />
  );
}
